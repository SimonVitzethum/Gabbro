-- erwartet: N057
-- Lane E1, narrowed by lane E2: the call resolves nowhere (no library
-- module in scope), so it is refused as UNRESOLVED (N057) -- once per
-- call, in statement and in binding position alike. A resolving call is
-- `N069` instead (`beispiele/gift/820`).
module gift805 {
impl fn f(a : u32) -> u32 effects { pure } costs <= 32 ops {
    @spirv#kernel(a) { dispatch 0 };
    let code = @spirv#kernel(a) { dispatch { nested } 0 };
    return a;
}
}
