-- erwartet: P001
-- Lane E1: a library call names its library and its function as
-- `@library#function` -- without the `#` there is no call.
module gift803 {
impl fn f(a : u32) -> u32 effects { pure } costs <= 8 ops {
    @spirv kernel(a) { dispatch 0 };
    return a;
}
}
