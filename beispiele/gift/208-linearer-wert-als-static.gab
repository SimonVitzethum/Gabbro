-- erwartet: N011
-- **Dieselbe Regel, der andere Speicherplatz.**
--
-- Ein linearer Wert hat Bytes (`SPRACHE.md`:721), aber keinen PFAD in einem `static`: er
-- entsteht dort beim Start und wird nie verbraucht. *Ein `static`, der eine Ressource haelt,
-- haelt sie fuer immer.*

module gift {

linear type Region;

static mut vorrat : Region = 0;

}
