-- erwartet: E002
-- `pure` heisst „fasst nichts an". Eine zweite Wirkung daneben ist ein Widerspruch,
-- kein Zusatz.
module gift {

type T = { x : u32 in 0 .. 10, };
impl fn beides(c : ptr<normal, rw> T) effects { pure, writes c } {
    c.x = 1;
}

}
