# MUSE-REPORT-159 (lane 159: shl/shr finding + statement-certificate coverage)

## 0. Result in one paragraph

(1) The lane-158 finding is closed: `CertExpr::Shl/Shr` carry the width
and print `(.shl w a b)` / `(.shr w a b)` exactly as Lean spells it;
`pruefe-termidentitaet.py` reports **53 match, 0 mismatch** (was 51/2).
(2) Statement-certificate coverage went from **17 to 47 of 253 bodies**
(measured with the current binary on the current tree; lane 155 counted
245 on its tree -- 99 `.gab` files today, denominator = block-body
functions in `gabbro certificate` output). **35 of the 47** printed
bodies are pasted into the new `grammatik/Grammatik/ZeugnisKorpus.lean`
and accepted by Lean `decide` (plus 2 more in `ZeugnisStmt104b.lean`
from lane 155). The 60-body target is **not reached**: every remaining
refusal class lacks a Lean shape or a `lean-g` form (listed in section 5).
That is a finding, not a weakening -- no shape was invented.

## 1. What was built

**Rust printer, expression half** (`crates/gabbro-check/src/certemit.rs`):
`Shl(u32, Box, Box)` / `Shr(u32, Box, Box)`; `print` emits the width
first (`(.shl 3 (.lit 3) (.lit 2))`); `cert_range` and the new
`shift_side` enforce the `h1 < 2^w` / `h2 < w` legs exactly as
`certRange` does. New refusal tests: value past the width, amount past
the width (each `None`, never a weaker claim). Standalone suite:
31 passed, 0 failed (was 28).

**Rust printer, statement half** (`crates/gabbro-check/src/certstmt.rs`,
codes CS001--CS009 untouched, none added):
- `bereich_von_typ`: bare machine words claim `breite_von`/`grenzen`
  (the `lean-g` `int_range` spelling); bounds resolve `const` names
  (like counts already did); sugared widths claim their exact N-bit
  range (`zucker_bereich`, so signed sugar resolves -- the desugared
  lower bound is a unary minus, not a literal).
- `drucke_expr` prints `div`/`rem` (M102), `band` (M137), `|`/`^`
  (smallest covering width = the `maske` bound, so the claim is the
  checker's range exactly), `<<`/`>>` (smallest width both legs admit;
  value and claim ignore `w`), `~` (as `a ^ (2^B - 1)` in the storage
  word -- unsigned non-literal only, exactly like `m1.rs` M137),
  lossless integer conversions (the bare argument term, like the
  checker; lossy ones refuse), `u32::max`-style limit words (the folded
  literal, `grenzwort`/`zucker_bereich`). Widths thread beside ranges
  (`Option<(u8, bool)>`); arithmetic results carry none (widths track
  declarations, so `~` over computed values refuses -- safe, booked).
- A failed leg is `CS005` (shape exists, instance has no range).

**Tests** (`crates/gabbro-check/tests/certstmt.rs`, 45 tests): replaced
`unranged_result_refused` and `division_refused` with positives (same
probes now print); added bare-word result/alias/let, bool-result
refusal, div/rem/band positives + leg-failure refusals, bor/bxor/shl/
shr positives + refusals, bnot print/signed/literal, conversion
print/lossy, grenzwort, signed-sugar. Exact terms pinned everywhere.

**Differential script** (`instrumente/pruefe-termidentitaet.py`): shl/shr
vectors carry the width, the print-arm check requires width-carrying
arms, mismatch demos are now match confirmations (must elaborate).

**Lean witnesses** (`grammatik/Grammatik/TermIdent104.lean`): comments
only -- witnesses 7-8 already spelled the width; CUTS reworded
MISMATCH -> MATCH. No proof content changed.

**Lean corpus paste** (`grammatik/Grammatik/ZeugnisKorpus.lean`, new,
generated by scratch script, imported in `grammatik/Grammatik.lean`):
27 pure-int prints over `refD` with hand-built contracts, 2 slot bodies
over the real 108 export, 6 slot bodies over hand-built minimal
declarations (59, 109, 112, 44) -- each a `decide` validity example
plus a soundness corollary. No named theorems (anonymous `example`s
only, like `TermIdent104`), CUTS + axiom note at the end.

## 2. Verification (last lines)

- `./cargo-pruef`: `== exit 0; failing tests: 0` (incl. 31 certemit,
  45 certstmt tests).
- `python3 instrumente/pruefe-termidentitaet.py`:
  `== counts: 53 match, 0 mismatch, 64 not-comparable (0 BAD rust-src lines)`;
  `RUST OK: 31 passed; 0 failed`; match confirmations shl/shr 0 errors.
- `./lean-probe grammatik/Grammatik/ZeugnisKorpus.lean`:
  `== 0 error(s) in the COMPLETE output`.
- `./lean-bau`: `Build completed successfully (145 jobs)`
  (`[143/145] Built Grammatik.ZeugnisKorpus`).

## 3. Coverage tables

Bodies printing (block-body functions with a printed term):

| stage | printed / 253 |
|---|---|
| lane 155 (baseline, re-measured with pre-change binary) | 17 |
| + bare words / aliases | 36 |
| + div/rem/band (+0 in corpus) / const bounds | 38 |
| + width package (`\|`/`^`/`<<`/`>>`/`~`/conversions/limits/signed sugar) | **47** |

Refusals per code (sums check against 253):

| code | before | after |
|---|---|---|
| CS001 (no statement shape) | 88 | 94 |
| CS002 (no expression shape) | 23 | 31 |
| CS003 (no integer range) | 20 | 14 |
| CS005 (unresolvable claim) | 105 | 67 |
| printed | 17 | 47 |

CS001/CS002 rise while CS003/CS005 fall: bodies travel further and
fail deeper (the lane-155 effect) -- the refusal is deeper, not new.

Lean acceptance of the 47 printed bodies:

| group | bodies | Lean `decide` |
|---|---|---|
| pure-int prints over `refD` | 27 | 27 green (`ZeugnisKorpus.lean`) |
| 108 slot bodies over real export | 2 | 2 green (`ZeugnisKorpus.lean`) |
| slot bodies over hand declarations (59/109/112/44) | 6 | 6 green (`ZeugnisKorpus.lean`) |
| 104 bodies (lane 155) | 2 | 2 green (`ZeugnisStmt104b.lean`) |
| pointer/call bodies (13/14/16/25/47/52/72) | 10 | open: need per-body `End104Args` proofs |
| **total accepted** | | **37 of 47** |

## 4. What I believe is wrong / weaker than asked

- The 60-body target is missed (47). Section 5 shows every remaining
  class lacks a Lean shape or a `lean-g` form; reaching 60 needs new
  Lean shapes (listed), which the task forbids inventing.
- `lean-g` exports 2 of 99 corpus files (104, 108). Pastes for other
  programs rest on hand-built places (contracts/declarations), exactly
  like lane 155's `?hp`/`Args` precedent -- `decide` accepts the
  pasted term, it does not re-derive it from the source (booked in CUTS).
- The `+=` desugar (`s += v` as `assignSlot(s, s+v)`, which `lean-g`
  confirms) was investigated and dropped: both corpus bodies are
  blocked deeper (bool/wrapping fields) -- 0 gain, so no code.
- Literal call arguments (36-asm): unpasteable (asm callee, no G form).
- `lean-bau` was green at commit; parallel lanes may drift registers
  (`pruefe-zahlen.py`) -- untouched here by design.

## 5. Next Lean tasks (frequent forms with no Lean shape, measured)

- `~`/`<<`/`>>` need widths the surface does not name (printer
  computes them; 61/62 print) -- a `CertExpr` width-inference or
  elaboration rule would make the print first-class.
- Branch refinement (`if n >= 1` narrows `n` for `/`, `z < GRENZE`
  for `z + 1`): 08-bereiche (6+ bodies) needs facts in branches.
- Returning `if` branches (`return` inside a branch): `CertSeq` has no
  return; 3 bodies.
- `locks` (21), `loop` (20), `match` (9), `narrow` (8), `let-else` (7),
  `awaits`/`exchange`/`publish`/`breaking`/`alloc`: no shapes.
- `-> bool` returns (10), bool/float locals and fields (30+): no
  bool-value shapes.
- `static` reads/writes (~10 incl. all `unknown name`): no G form
  (`LG001 static has no G form`) -- unpasteable before Lean names them.
- Generated table ops (`Verzeichnis::insert`, 5): no callee declaration.
- Device/register/asm bodies (~10): no G form.
- Coarse `shr`/`shl` claims (`(0, h1)` vs the checker's corners):
  `falte` prints a valid term that does not fit its annotation.

## 6. Rule-13 / rule-12 notes

- No new Lean theorems with universal premises over program syntax:
  `ZeugnisKorpus.lean` and `TermIdent104.lean` contain anonymous
  `example`s only (plus `def` places/terms), so no `_zeuge`
  companions are owed. The task names no `ZEUGE:` target.
- Every new Rust refusal path is pinned in-test; no test was deleted
  (two outdated refusal tests were replaced by positives on the same
  probes, the lane-155 precedent).
- Refusal codes CS001--CS009: no conflicts (none added, none renamed).

Commits (branch `muse/159`): `14423c67` (Rust + probes),
`a9f0d2b7` (differential script + witnesses),
`53c6a52d` (corpus paste). This report follows as 4/4.
