/* Der EIGENE fremde Pruefkoerper des Schreibers -- in einer statischen Bibliothek,
 * so wie ein Projekt ihn ueblicherweise ausliefert. */
#include <stdbool.h>
bool pruefe_kontostand(void) { return false; }
