-- emission-143: pins `static inline` (Census C2, OffeneForm.cInline, 440 sites).
-- A bare format declaration emits the byte-order helpers (LESER_C/SCHREIBER_C)
-- and one reader/writer pair per field, all `static inline`.
-- Verdict: admit with price (ruled in messung/CFORM-REGEL-INLINE.md). No emitter change.

module emission143::cinline {

format Kopf endian little {
    magie : u32,
    version : u16,
}

}
