# The reach of the Lean channel — how far is the tool that proves an ARBITRARY Gabbro program?

**Status: measurement in progress.** Written at the start of the run so that a lane that dies
mid-run loses its remaining numbers and not its measured ones. Every section below is either
a MEASURED number with the command beside it, or is marked `NOT YET MEASURED`.

*Tree: `a053d3a`. Server: `ki-pc-fisch-101`, `gabbro-le`. `lean` at `~/.elan/bin/lean`
(NOT on `PATH` — `which lean` says `command not found`; `pruefe-lean-beweis.sh` finds it
anyway because its default is the absolute path).*

## The answer, in one paragraph

*NOT YET MEASURED — filled in at the end.*

## Evidence

*Sections filled in as measured.*
