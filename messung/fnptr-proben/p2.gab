module probe::zwei {

type Treiber = { bereit : fn() -> bool, };

impl fn wahr() -> bool
    effects { pure }
    costs   <= 1 ops
{
    return true;
}

impl fn baue() -> bool
    effects { pure }
    costs   <= 2 ops
{
    let t : Treiber = Treiber(bereit: &wahr);
    return true;
}

}
