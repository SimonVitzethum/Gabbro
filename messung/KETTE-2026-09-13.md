# Chain count, 2026-09-13

The first booking of the metric PLAN-UEBERSETZUNGSVALIDIERUNG.md section 3 defines:
how many corpus programs pass the WHOLE chain. Measured with
`instrumente/zaehle-kette.py` (lane 163) over the 101 tracked `beispiele/*.gab`,
binary `target/debug/gabbro` built from this tree on ki-pc-fisch-101.

## Headline

**CHAIN COUNT: 0 of 101.** No program passes every checked sieve. The column that
guarantees it is (e): T2 does not exist, so no correspondence certificate exists and
none is Lean-checked. With `--lean` (pin files re-checked through `./lean-probe`,
`./lean-bau` green, 149 jobs) column (a) measures 1: `104-referenz.gab` reads
`[YYYY0]` -- one checked Lean certificate (e) away from a closed chain.

## Sieve totals (of 101)

| sieve | pass | what it means |
|---|---|---|
| (a) Lean parse [pin] | 1 | only `104-referenz.gab` has a generated pin (`Export104.lean`, `Parser/Uebersetze.lean` `u104*`); `./lean-probe` green on both pin files with `--lean` (default run: pin present, Lean not re-run, counts as not passed) |
| (b) `gabbro lean-g` export | 3 | `104-referenz`, `108-disjoint-start-locks`, `118-sperrinvariante-erhaltung` |
| (c) `gabbro certificate`, every body | 14 | the 3 above plus `06-annahmen`, `07-eintritt-und-boot`, `11-grammatikbefunde`, `20-falle-vier`, `24-ip-kopf`, `25-entrust`, `51-abwesenheit-und-absage`, `60-annahme-mit-maschine`, `67-befehlsebene`, `69-integer-conversion`, `73-sugar-widths` |
| (d) C forms lemma/assumption | 57 | every emitted form is state (i) or (ii) of `pruefe-cformen.py`; 44 fail on state-(iii) forms, none on unclassified statements |
| (e) correspondence checked | 0 | T2 does not exist (`Erhaltung.lean` holds the `CorrCert` SHAPE, not the rechecker; `gabbro` prints no per-program sidecar) |

Row patterns (`[abcde]`, `.` = not measured) with `--lean`: `Y`YYY0 for 104,
`.FFF` 41, `.FFY` 46, `.FYY` 8, `.YYY` 2 (108, 118), `.FYF` 3 (default run without
`--lean`: (a) reads `.` everywhere, 104 reads `[.YYY0]`). All 101 programs emit (0 emitter refusals, 0 checker-error
aborts on `certificate`); the 87 failing (c) rows carry 207 `REFUSED CSnnn` bodies.

## What the numbers say

* The sieves are NOT nested. 11 of the 14 certificate-clean programs fail `lean-g`
  (`.FYY`): the G fragment (`LG001` and kin refuse every surface form without a G
  counterpart) is the narrowest sieve after the missing T2. 46 programs emit only
  lemma/assumption-covered C yet fail both `lean-g` and `certificate` (`.FFY`).
* The three `.YYY` programs -- 104, 108, 118 -- pass everything that can pass today.
  104 is the program T2-minimal is scoped to (close one chain first); its row reads
  `[.YYY0]`, i.e. one checked Lean certificate (e) away from a closed chain.
* Column (d) uses the three C-form states booked the same day: 1797 lemma
  occurrences, 187 named-assumption occurrences (`stmt:asm` -> `AxCorr`;
  `stmt:reg-load`/`expr:volatile` -> `hdev`, `RegLokal`; `stmt:reg-store` ->
  `RegLokal`), 367 without-semantics occurrences, 0 unclassified.

## How to re-measure

`python3 instrumente/zaehle-kette.py` (add `--lean` to re-check the 104 pin through
`./lean-probe`; the count stays 0 until T2 exists). Exit 0 once it measured, 2 when
it measured nothing. The per-program table is the script's stdout, not this file.

## 2026-09-14: after lane 164 (corr-lean) and the per-program column (e)

Measured with `zaehle-kette.py --lean` on master 1518d8a5 (ki-pc-fisch-101): **every
sieve passed separately: 1** (`104-referenz.gab`, `[YYYYY]`: pin re-checked, lean-g,
every body certified, all C forms lemma/assumption, `corr-lean` printed with no
refusal and `Korrespondenz104.lean` re-checked green). **CHAIN COUNT: 0** -- the
counter now counts a chain as closed only where a `Schlusssatz*.lean` names the
program and re-checks green, because the parsed program (`gP`), the certified program
and the program the C corresponds to are not yet proved to be one. Sieve totals:
(a) 1, (b) 3, (c) 14, (d) 57, (e) 1 of 101.
