# Handover, 2026-09-03 — state at `10e6340`

*Written before a context compaction. Everything here is measured; the command stands beside
each number in the report it came from.*

## The one decision that blocks work right now

**`F03` is the last fragment without a Durchstich (`H = 1`), and it cannot proceed without
the owner.** The `H = 0` lane stopped in front of it as instructed.

`traverse … over mappings of` has no lowering, and the reason is not a defect:

> It needs a **named resolver from frame to readable node**. The generated C takes it as the
> parameter `knoten_zu`. **Gabbro has no clause to write it.**

That is «B10»-shaped — a language question, not a repair. `F03` carries 27 errors over seven
codes on top of it. **Cost if it goes ahead: 20–40 h with an open end.**

Two smaller owner items stand beside it, unchanged: **`state`** (do not build, extend the
refusal by exactly two lines — the only red guard that is a decision) and the **release**
(`origin/master` is ~95 commits behind, the `0.0.1` tag is local only, and the AUR checksum is
measurable only after a push; the push was refused to the agent and needs the owner).

## Where `H` stands, and why the three repairs were worth more than the metric

`H` 4 → 1. **In all three, the refusal was right and the fragment was wrong — and each hid a
second defect in the checker or the emitter:**

* `F09` — `walk … levels EBENEN` refused a `const` NAME. Third site of a named `W7` class.
* `F01` — **`or R` at an `impl fn` without a result had no C at all.** The success path's
  `return true;` was never written; it compiled clean at `-O0` **and** `-O2`. *The only
  instrument that sees it is a run* — which is the strongest argument for `H` counting
  Durchstiche rather than compilations.
* `F05` — `N046` never read `f.fehler`, so `extern fn abs(a : i32) -> i32 or R` passed with
  `0 errors` and emitted a one-parameter prototype called with three arguments.

**The tool's caveat is unpaid on purpose:** the ten fragments are chosen for their DIFFICULTY,
so `H = 0` over them stays trap 80 until a corpus stands beside them that nobody looked at
while building. `H = 1` improves the tree; it is not a statement about the language.

## GabbroV: the gates that are through, and the two that are not

* **`V1` through** — 56 of 66 obligations sayable in the Lean fragment (after the rebooking:
  55 of 63). `G1` **has no threshold**, so no count clears it; the sentence *"`G1` does not
  fire"* is withdrawn and mechanised as `WG1`.
* **`V2a` (vacuity) built** — 0 of 31 obligations with a precondition are vacuous, and it is
  **not a bounded search**: `vacuous_sound` proves no model exists at all. **`V2b`
  (satisfiability) not started** — the eight assumptions are German prose sentences and `G5`
  can neither fire nor be cleared.
* **Gate 2: three of five drawn obligations REFUTED**, confirmed by blind re-encodings written
  by fresh agents with no context, each with a control returning the opposite verdict.
  **The corpus is wrong three times over**, and Z3 answered in 16–21 ms.
* **The reachability wall was an ENCODING**: a bound-free rank form answers the corpus's own
  `NSLOTS = 80256` in 0.034 s at 1 371 bytes, against 16 KB at the bound that timed out.
* **Trust-base point 6 has zero lines** — no Lean→SMT translator exists. And **Gate 2 exercised
  the direction design C does not need**: three `sat`s need no certificate, the two `unsat`s
  were never certificate-checked. `G2` is fully open.

## The manifest — a lane is on it now

Measured: **four of ten fragments emit no manifest at all, 10 obligation lines against a
population of 63, and not one of the five Gate 2 rows has a manifest line.** So the audit that
refuted three obligations **never went through the manifest**. `§15`'s *"Nothing is silently
lost"* does not hold today.

The ordinal defect (`ensures #1`) is real and reproduces byte-for-byte — **but the same binary
already prints the disambiguating term under `--lean`**, so it is a dropped field, not a new
computation.

## The class this week keeps producing, booked six times

> **A merge that would have to ADD numbers has no text conflict.** Two lanes move one metric by
> one for different files; git sees two identical digits and is satisfied; both sides are green
> alone and the merged tree is wrong.

Sixth instance today: poison probes and `MARKE_EMIT_M`, both predicted by the lane in advance
and both confirmed by re-measuring (437 and 59). **The only grip that works is the run in the
merged tree.**

## Beta, without `F03`

`§49` `B1`–`B5` are through. What remains: **`§50` #6** (pull the fifth mark — the split
*unwritten* versus *inexpressible* is the work), **`B5`'s other half as a booking**, and the
owner's two. Roughly **3 h plus the owner's minutes** — and `F03` on top if it goes ahead.

Behind all of it stands the **unseen port**, which is not hours: *it is the only measurement
whose outcome re-evaluates the Beta list itself.*
