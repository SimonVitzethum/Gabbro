# `A_p` — the probe quota, and the floor that was paid on 2026-09-04

**This file retires `K100`'s gate `A = 19`.** It says what replaces it, what the replacement
measures, and what the tree measures against it **today**.

**It was written RED on purpose** — *a bound the tree already meets is a decoration and not a
gate* — and it names its price in four programs. **The four were written on 2026-09-04 and
the floor is met with less than nothing to spare: `5 of 39 = 0.1282` against `1/8 = 0.125`.** Every one
of them carries a positive control that was made to fall, because *a probe whose control
cannot be made to fall is not finished*:

| probe | control | what made it fall | it fell |
|---|---|---|---:|
| `sonde_mxcsr_rne` | three violating rounding modes, set in MXCSR | `ldmxcsr` inside, and an `LD_PRELOAD` constructor outside | **600 000** of 600 000 |
| `sonde_keine_ueberbreite` | the same expression forced onto the x87 stack | `fldl`/`faddp`/`fstpl`, and a build with `-mfpmath=387` outside | **200 000** of 200 000 |
| `sonde_tsc` | a frozen source and a backward one through the same detector | the counter is read through a function pointer | **400 000** of 400 000 |
| `sonde_rdtscp` | frozen, backward, and one outside the `rdtsc` bracket | the same, plus `prctl(PR_SET_TSC)` outside for the **77** arm | **600 000** of 600 000 |

**The floor no longer forbids nothing, it forbids the next assumption arriving without its
probe** — and there is no `P4` row left to write, so the next one that does has to move the
apparatus and not this number.

    ./instrumente/pruefe-sondendeckung.py

---

## Why `A = 19` is retired

`dokumente/PLAN.md` fixes the target and its arithmetic in one sentence: *„`gabbro annahmen`
stand beim Schreiben dieses Absatzes bei ~~**14**~~; nach K100 muss dort **19** stehen"* —
**19 = 14 + the five rebookings of `K100.2`**. `DONE.md` records the day it was hit.

**So 19 was never a bar over the trust surface. It was a prediction of a delta, and it came
true on 2026-08-17.** What has happened since is that its measurand grew: 44 names over 40
distinct sentences, four of them generated by the checker and declared in no `.gab` at all.
`messung/AXIOMSCHICHT.md` §8.4 measured that and drew the conclusion this file acts on:

> **A target that a new example file can miss is pointed the wrong way.** Adding
> `beispiele/65-port-space.gab` raised `A` by two, and nothing about the trust surface got
> worse — the file made an existing machine dependency EXPLICIT, which is the movement the
> axiom layer exists for.

*Under `A = 19` as a gate, the cheapest way to pass is to write fewer programs, and the second
cheapest is to write `unfalsifiable` twenty-five times.* The second exit is latched by
`dokumente/UNFALSIFIZIERBAR.md`. **This file latches the first, and it latches the state
between them.**

---

## The three states, and the one that had no guardian

An assumption is in exactly one of three states:

| state | today | who watches it |
|---|---:|---|
| **unfalsifiable**, under a criterion | 6 clauses / 1 admitted | `pruefe-unfalsifizierbar.py` — a ratchet DOWN |
| **falsifiable, probe stands as a program** | **5 of 39** | this file — a ratchet UP |
| **falsifiable, probe MISSING** | **33 of 39** | *nobody, until this file* |

**The third row is the whole reason this document is not two ratchets.** A ratchet on the
unfalsifiable count does not fall when nobody writes a probe; a ratchet on the probe count
only rises when somebody does. **The state the tree is actually in is the one neither of them
can see** — and it was 37 of 38 when this file was written. It is 33 of 39 today.

*A count cannot watch it either*, and for exactly the reason `A = 19` failed: a count grows
with the corpus, so writing one more example program makes the number worse without making the
tree worse. **So the guarded figure is a PROPORTION.**

---

## `A_p`, defined

> **`A_p` = falsifiable assumptions whose named probe stands as a program in `sonden/`,
> divided by falsifiable assumptions.**

Both halves come out of the same scan, and both are named here rather than left to a reader:

* **The corpus is `beispiele/` plus the entries the CHECKER generates.** That is exactly what
  `gabbro annahmen beispiele/*.gab` prints — the assumption set Gabbro carries outward, and
  the same measurand `A` had. It is deliberately narrower than
  `pruefe-unfalsifizierbar.py`'s corpus, which reads every non-poison `.gab` in the tree: a
  `falsifier` in `messung/proben/` is an input written to make the checker say something, and
  counting it here would inflate the denominator with debt nobody carries. **The wider figure
  is not dropped, it is reported beside the quota** — see *What stands outside* below.
* **A falsifiable assumption** is one whose declaration carries `falsifier <probe>`. The
  grammar admits `assume`, `axiom` and `retires`, and the third one names its assumption
  after the function (`manifest.rs::stilllegungsannahmen`), which is why row 25 below has a
  name no `.gab` contains.
* **"Stands as a program"** means `sonden/<probe>.c` exists. Nothing about whether it runs,
  and nothing about whether it runs green — `sonden/README.md` fixes the contract, and return
  code **77** (*not runnable here*) is a full member of it.

```bash
./instrumente/pruefe-sondendeckung.py
# ==   5 of 39 falsifiable assumptions carry a probe that stands as a program
```

**And the denominator is not free either.** Three ways exist to keep `A_p` from falling when
an assumption is added, and only one of them is work:

1. **write the probe** — the quota holds;
2. **write `unfalsifiable`** — the assumption leaves this denominator and enters
   `dokumente/UNFALSIFIZIERBAR.md`'s, whose ADMITTED count **may not rise**. The exit is
   latched, and it costs a row under a criterion that must already be written down;
3. **do not declare the machine dependency at all** — and *nothing here catches that*. No
   guardian over a register can see a sentence nobody wrote. It is named because the honest
   limit of this file is that it measures declared assumptions, not real ones.

---

## The floor — and it is the whole point

**A proportion-ratchet alone forbids regression and demands nothing.** At 1 of 38 a ratchet
booked at that level would have passed forever while nothing was written. So `A_p` carries a
**floor** as well as a ratchet, and the floor was chosen so the tree **failed it on the day it
was written**.

> ### `A_p ≥ 1/8`
>
> **When this was written `A_p = 1 / 38 = 0.026` and the tree missed the floor of 0.125 by a
> factor of 4.75. The price was four programs; they were written on 2026-09-04, and
> `A_p = 5 / 39 = 0.1282` MEETS the floor with 0.0032 to spare.**
>
> *The slack is the smallest a unit fraction leaves, and that is the point:* one more
> falsifiable assumption without a probe puts `A_p` at `5 / 39 = 0.1282`, still above; two put
> it at `5 / 40 = 0.125`, exactly on; **three break it.** The floor stopped being a debt and
> became a brake, and the ratchet at `5/38` bites one assumption earlier than that.

### Where `1/8` comes from — it is not a round number

`sonden/README.md` and `messung/RACE.md` §5.4 already carry the classification this floor
rests on: for each named probe, **what it would need in order to exist**. Four classes, and
they are read from there rather than invented here:

| | what the probe would need | of 39 |
|---|---|---:|
| **`P1`** | **ring 0** — a control register, a page table, an MSR, an `in`/`out` | 14 |
| **`P2`** | **a device** — VT-d, virtio, a 16550, a timer, a counter | 16 |
| **`P3`** | **a mechanism the generator does not emit** — a grace period, a reader that fetches, a source that ends | 4 |
| **`P4`** | **nothing — it runs in userland on the bench this folder already has** | **5** |

*The classification is an ESTIMATE per name and not a measurement; `sonden/README.md` says so
about its own table, and that caveat travels with these rows.* What is not an estimate is the
consequence:

> **`P4` is the work that needs no device, no privilege and no decision by the owner.** Five
> rows stand in it, **all five have a program since 2026-09-04**, and there is nothing left in
> the class to write.
>
> `1 + 4 = 5` of 38 is **0.1316**. The largest unit fraction below it is **`1/8` = 0.125**;
> `1/7` = 0.143 is already out of reach. **So `1/8` is the tightest simple bound that the work
> which is actually possible reaches — and the tree reaches it exactly.**
>
> *What that costs to say again:* the class is now EMPTY of open work, so the floor can no
> longer be met by writing another userland probe. **The next assumption that arrives without
> one is answered by `unfalsifiable` under a written criterion, by a bench with ring 0 or a
> device — or not at all.** Tooth 8 says so in a number rather than in this sentence.

### What reaching the floor cost — and it was paid on 2026-09-04

**Four probes.** Each is a userland C program under `sonden/`, each needs a sensitivity arm
(`sonden/README.md`: *„Wer keine Empfindlichkeitsprobe hat, hat keine Sonde"*), and each had
to be entered in `manifest::SONDEN_MIT_PROGRAMM` or the checker keeps striking its name:

| probe | assumption | what it does | control, and what it took to make it fall |
|---|---|---|---|
| `sonde_mxcsr_rne` | `gleitkomma_rundungsmodus_ist_rne` | read MXCSR **and** the x87 control word, and round two ties that separate all four IEEE modes | the three violating modes are set with `ldmxcsr` and must be caught in every round; from outside, an `LD_PRELOAD` constructor set each of them and the probe exited **1** three times |
| `sonde_keine_ueberbreite` | `gleitkomma_x86_rechnet_mit_sse2` | `(1 + 2^-53) + 2^-53`, which is `1` in 64 bits and `1 + 2^-52` in 80 | the same sum forced onto the x87 stack with `fldl`/`faddp`/`fstpl` must differ every round; from outside, the identical source built with `-mfpmath=387` exited **1** |
| `sonde_tsc` | `rdtsc` | hold one core, then read the counter through a function pointer and fall on a standstill **or** a step back; **77** if `CR4.TSD` forbids it | a frozen source and a backward one run through the same detector and must both be caught; from outside, `prctl(PR_SET_TSC, PR_TSC_SIGSEGV)` drove it to **77** and a build with the frozen source in arm 3 to **1** |
| `sonde_rdtscp` | `rdtscp` | `CPUID` leaf `0x8000'0001` for the `requires`, then the instruction bracketed between two `rdtsc` reads; **77** where the feature is absent | three sources violate by construction — frozen, backward, outside the bracket; from outside, `PR_SET_TSC` gave **77**, a cleared feature bit gave **77**, and a foreign clock in arm 4b gave **1** |

**Measured: 5 of 39 = 0.1282 ≥ 0.125.** The floor is met and not exceeded — *by design,
because a floor with slack in it is a floor nobody has to think about again.*

> **Not one of the four is an analogy** (`sonden/README.md` forbids those). Each forces the
> condition the assumption denies: a real rounding mode in a real register, a real x87
> stack, a real counter that a real `prctl` takes away. **The one thing no userland probe
> can force is a hardware counter that genuinely stops** — so `sonde_tsc` and `sonde_rdtscp`
> force it at the source they read through, which is the same move `sonde_release_sichtbarkeit`
> makes in its arm 3 and `sonde_boot_unerreichbar` in its arm 1. *That limit is written into
> both programs rather than left for a reader to notice.*

**And one line is free but does not help.** `release_stellt_sichtbarkeit_her` is booked
`unfalsifiable` under a reason `dokumente/UNFALSIFIZIERBAR.md` REFUSES (`R1`), and the program
that would refute it already stands in `sonden/`. Rebooking it to
`falsifier sonde_release_sichtbarkeit` costs no probe, closes the orphan below, and moves
`A_p` to **6 of 39 = 0.1538**. *It was the right change when the floor was missed by 4.75x and
it is still the right change; it was never the price.*

### What this floor is NOT

**It is not a target, and it is not enough.** It was the first bound the tree failed, and it
is derived from the only work that requires no new hardware, no kernel and no decision. The 29
rows in `P1` and `P2` stay exactly where they are: a probe for `write_cr3` needs ring 0, and
whether this project ever gets a bench with ring 0 is a question about apparatus that no
guardian can force. **`1/8` said what could be demanded then. It says nothing about what would
be enough** — and paying it changed the accounting by 4 rows out of 38, which leaves **33
assumptions whose probe is still only a name.**

*The floor may be RAISED and may not be lowered.* A run that fails
[`the reachability tooth`](#the-teeth) — the floor no longer reachable even with every `P4`
probe written — is a finding about the corpus growing faster than the bench, and the answer to
it is a decision about apparatus, **not a smaller number here.**

---

## The other direction — a program nobody names

**A built program that no `falsifier` references is the same build type as a name without a
program, just reversed.** Both are a connection with one end missing, and both are held
here.

```bash
ls sonden/*.c | wc -l                                                # 6
for f in sonden/*.c; do n=$(basename "$f" .c)
  grep -rqI "falsifier $n" --include=*.gab . || echo "$n"; done   # 1
# sonde_release_sichtbarkeit
```

`sonde_release_sichtbarkeit.c` exists, runs, and carries a positive control that fell 3 079
times in 1,5 million rounds. **No line in any `.gab` names it.** `sonden/README.md` said so on
the day it was built — *„Bis die Zeile in `beispiele/` steht, ist sie eine Sonde ohne
Verpflichtung"* — and `N024` (*a probe belongs to exactly ONE obligation*) has nothing to hold
it to.

| | program | why no `falsifier` names it | since |
|---:|---|---|---|
| 1 | `sonde_release_sichtbarkeit` | `release_stellt_sichtbarkeit_her` is booked `unfalsifiable`, so the grammar has no slot for the name. The reason is REFUSED under `R1` in `dokumente/UNFALSIFIZIERBAR.md`; the repair is one line in `beispiele/06-annahmen.gab` | 2026-08-21 |

**1 orphan program is booked. The mark may fall and not rise.** Unlike the quota this one is a
plain count, because it is not a share of anything: every program in `sonden/` should be bound
to an obligation, and the target is zero.

---

## The register

**38 rows, one per falsifiable assumption of the corpus, and every column is checked.** The
set of rows must equal the set the scan finds — *a row cannot be created by editing this
table, and an assumption cannot be added without a row appearing.* The probe name must be the
one the corpus declares; the class must be one this document defines; the program column must
agree with what stands in `sonden/`.

*No line numbers.* Over 38 rows in 14 files a line column produces a stream of drift
notices and not one finding — the model in `dokumente/UNFALSIFIZIERBAR.md` carries one
because eight rows in three files are cheap. **Every column here is a hard check
instead** — and **no assumption may stand twice**, because set equality compares sets and
a doubled row would still be counted where the floor's reachability is decided.

| # | assumption | probe | class | program |
|---:|---|---|---|---|
| 1 | **cr3_leert_den_tlb** | `sonde_cr3_leert_den_tlb` | `P1` | -- |
| 2 | **dma_kohaerent** | `sonde_dma_kohaerenz` | `P2` | -- |
| 3 | **dma_sichtbarkeit_in_reihenfolge** | `sonde_dma_reihenfolge` | `P2` | -- |
| 4 | **dma_veroeffentlichung_braucht_barriere** | `sonde_dma_ohne_barriere` | `P2` | -- |
| 5 | **eingabe_endet** | `sonde_eingabe_endet` | `P3` | -- |
| 6 | **gast_bleibt_in_seinem_raum** | `sonde_gastausbruch` | `P1` | -- |
| 7 | **geraet_antwortet** | `sonde_geraet_antwortet` | `P2` | -- |
| 8 | **geraet_quittiert** | `sonde_vtd_srtp` | `P2` | -- |
| 9 | **geraeteregister_veroeffentlicht_wie_ein_atomic** | `sonde_virtio_avail` | `P2` | -- |
| 10 | **gleitkomma_rundungsmodus_ist_rne** | `sonde_mxcsr_rne` | `P4` | **PROGRAM** |
| 11 | **gleitkomma_x86_rechnet_mit_sse2** | `sonde_keine_ueberbreite` | `P4` | **PROGRAM** |
| 12 | **gnadenfrist_ist_abgelaufen** | `sonde_leser_noch_drin` | `P3` | -- |
| 13 | **invlpg** | `sonde_invlpg` | `P1` | -- |
| 14 | **karte_antwortet** | `sonde_karte_antwortet` | `P2` | -- |
| 15 | **karte_liest_nach_dem_index** | `sonde_deskriptor_zu_frueh` | `P2` | -- |
| 16 | **kern_bleibt_unter_jeder_aenderung_abgebildet** | `sonde_kern_entmappt` | `P1` | -- |
| 17 | **leser_holt_ab** | `sonde_leser_holt_ab` | `P3` | -- |
| 18 | **masks_irq_schuetzt_wie_eine_sperre** | `sonde_irq_maskiert` | `P1` | -- |
| 19 | **mmu_folgt_ihrem_modell** | `sonde_pf_bei_p0` | `P1` | -- |
| 20 | **neuer_eintrag_verdraengt_nichts** | `sonde_praesent_ohne_invalidierung` | `P1` | -- |
| 21 | **portraum_ist_x86** | `sonde_portraum` | `P1` | -- |
| 22 | **quelle_endet** | `sonde_quelle_endet` | `P3` | -- |
| 23 | **rdtsc** | `sonde_tsc` | `P4` | **PROGRAM** |
| 24 | **rdtscp** | `sonde_rdtscp` | `P4` | **PROGRAM** |
| 25 | **stilllegung_boot_ende_ist_unerreichbar** | `sonde_boot_unerreichbar` | `P4` | **PROGRAM** |
| 26 | **tlb_ist_nach_cr3_leer** | `sonde_tlb_nach_cr3` | `P1` | -- |
| 27 | **uart_leert_sich** | `sonde_uart_leert_sich` | `P2` | -- |
| 28 | **virtq_geraet_schreibt_used** | `sonde_virtq_used` | `P2` | -- |
| 29 | **vtd_srtp_quittiert** | `sonde_vtd_srtp` | `P2` | -- |
| 30 | **vtd_te_wirksam** | `sonde_vtd_te` | `P2` | -- |
| 31 | **write_cr0** | `sonde_cr0` | `P1` | -- |
| 32 | **write_cr3** | `sonde_cr3` | `P1` | -- |
| 33 | **write_cr4** | `sonde_cr4` | `P1` | -- |
| 34 | **wrmsr_efer** | `sonde_efer` | `P1` | -- |
| 35 | **x2apic_braucht_zwei_schritte** | `sonde_x2apic_braucht_zwei_schritte` | `P1` | -- |
| 36 | **zaehlwerk_antwortet** | `sonde_zaehlwerk_antwortet` | `P2` | -- |
| 37 | **zeitgeber_meldet_sich** | `sonde_zeitgeber_meldet_sich` | `P2` | -- |
| 38 | **zeitgeber_tickt** | `sonde_zeitgeber_tickt` | `P2` | -- |
| 39 | **frist_zaehle_werte_eingehalten** | `sonde_tick` | `P2` | -- |

**39 rows, 5 of them with a PROGRAM, 5 of them `P4`** — and one probe name carries two rows
(`sonde_vtd_srtp`, rows 8 and 29), which is why 39 assumptions stand under 38 names.

---

## What stands outside

`falsifier` clauses live in the tree beyond `beispiele/`, and they are counted here so the
narrower denominator above cannot hide them:

```bash
grep -rhoI "falsifier [a-zA-Z_0-9]*" --include=*.gab . --exclude-dir=gift \
  | sed 's/falsifier //' | sort -u | wc -l
# 48
```

**48 distinct probe names stand in the whole non-poison corpus, and 35 of them are in the
register above** — the register's other two, `sonde_mxcsr_rne` and `sonde_keine_ueberbreite`,
are GENERATED by the checker and written in no `.gab` at all, which is why it carries 37.
**The other 13** stand in `messung/fragmente/` (a frozen Caprock excerpt), `messung/proben/`
(inputs written to make the checker speak) and `messung/treiber/`. *None of them has a
program either*, and the count is booked so it cannot grow in silence.

---

## The teeth

**Ten over the register, and four more that keep the READER honest** — and each one can come
out wrong. `pruefe-sondendeckung.py` runs **19 speech tests**, at least one per tooth and both
directions where a tooth has two, and the last of them is the control that keeps the other
eighteen from passing over a guardian that always says no.

| | tooth | what it forbids |
|---:|---|---|
| 0 | the subject exists | a green verdict over a missing document or an empty `sonden/` (`W17`) |
| 1 | rows == the corpus, both ways | an assumption added without a row; a row kept after its clause is gone |
| 2 | no assumption twice in the register | a doubled row — *set equality does not see it, and it is still counted where tooth 7 decides* |
| 3 | every row names the probe the corpus declares | a register that drifts from the source it describes |
| 4 | every row's class is defined here | a class invented at the moment of classification |
| 5 | the program column agrees with `sonden/` | a row that claims a program that is not there, or hides one that is |
| 6 | **`A_p` ratchet** | the quota falling — *an assumption added without its probe* |
| 7 | **`A_p` floor `≥ 1/8`** | a quota that is allowed to stay where it is. **It failed until 2026-09-04 and is MET since**, by 0.0066 |
| 8 | the floor stays reachable | a corpus that grows past what a bench without ring 0 can ever cover |
| 9 | orphan programs `≤ 1` | a second program bound to no obligation |
| 10 | `SONDEN_MIT_PROGRAMM` == `sonden/*.c` | **coverage claimed in the checker with no program behind it** |

And the four over the reader, because a scanner that has gone blind prints the same `ALL PASS`
as a clean tree (`W17`):

| | tooth | what it forbids |
|---:|---|---|
| a | the free class still says *userland* | the floor left standing over a class redefined beneath it |
| b | construction sites of `Klasse::Falsifizierbar` in `manifest.rs` == 5 | a generated entry written in a shape the scanner cannot see |
| c | probe names outside this register == 13 | the narrowing of the denominator growing quietly |
| d | every `falsifier` hangs under an `assume`, `axiom` or `fn` | a clause the scan cannot attribute, silently dropped |

Tooth 10 is the one that makes the rest load-bearing. `manifest::gedeckt` decides from a
constant list whether a probe name stands or is struck; without this tooth, adding a string to
that list would raise `A_p` **with no program written at all** — the same move as writing
`unfalsifiable`, one layer down and invisible from here.

**What no tooth does.** Whether a probe would actually refute its assumption is nobody's
script: `pruefe-sonden.sh` says so about itself (*„Was hier gemessen wird, ist DASS eine
laeuft, nicht WORUEBER"*), and the class of a row is an estimate a reader may dispute. What is
excluded is the pair of things a script can exclude: **that the quota falls unseen, and that a
row stands under a class nobody wrote down.**
